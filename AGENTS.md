# AGENTS.md

The onboarding map for this project lives in **[CLAUDE.md](./CLAUDE.md)** — read
that first. It explains the architecture, conventions, how to run the platform,
the extraction engine, and where to find everything, without needing a full
repo scan.

This file exists so agents that look for `AGENTS.md` (instead of `CLAUDE.md`)
find their way there too. The two are kept in sync by pointing at one source:
CLAUDE.md is authoritative; update it, not this file.
